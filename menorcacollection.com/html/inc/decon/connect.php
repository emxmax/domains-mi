<?php

require("vars.php");  
/*require("NotORM.php");

if(driver=='mysql') $pdo = new PDO(driver.":host=".servidor.";dbname=".dbase, usuario,clave,array(PDO::MYSQL_ATTR_INIT_COMMAND => "SET NAMES utf8"));
else $pdo = new PDO(driver.":host=".servidor.";dbname=".dbase, usuario,clave);

$pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_WARNING);
$pdo->setAttribute(PDO::ATTR_CASE, PDO::CASE_LOWER);
$db = new NotORM($pdo);
$db -> debug = false;*/

date_default_timezone_set('America/Lima');

?>